# Core Framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0
python-multipart>=0.0.9
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ML / Computer Vision (torch installed separately in Dockerfile as CPU-only)
ultralytics>=8.2.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# HTTP client (for URL detection)
httpx>=0.27.0

# Database (SQLite only on HF Spaces)
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
alembic>=1.13.1

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.2
